---
import RecruitmentFormClient from './react/RecruitmentFormClient';

interface Props {
  preselectedSessionId?: string;
}

const { preselectedSessionId } = Astro.props;
---

<RecruitmentFormClient client:load preselectedSessionId={preselectedSessionId} />